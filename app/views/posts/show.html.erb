<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @post.text %>
</p>


<%= link_to 'back', posts_path %>
<% if signed_in? %>
<%= link_to 'Edit', edit_post_path %>
<%= link_to 'Destroy', post_path,
                  method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
